# Leave unset to auto-derive from the page's own hostname (works for phone/LAN access).
# Only set this if your backend runs on a different host/port than the frontend.
# VITE_API_BASE_URL=http://localhost:8000
