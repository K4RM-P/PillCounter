__pycache__/
*.pyc
.venv/
venv/

node_modules/
dist/

*.db
.env
.DS_Store
# Training-run checkpoints (many per run, not meant to ship) stay ignored;
# the production weight files under backend/ml/weights/ are tracked since
# the deployed app loads them directly at startup (see app/config.py
# MODEL_WEIGHTS_PATH / MODEL_VERSIONS) and there's no separate model
# download/artifact step in the deploy pipeline.
backend/ml/runs/
backend/ml/data/
